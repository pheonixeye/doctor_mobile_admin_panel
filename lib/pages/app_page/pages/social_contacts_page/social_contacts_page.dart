import 'package:flutter/material.dart';

class SocialContactsPage extends StatelessWidget {
  const SocialContactsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('social_contacts_page'),
      ),
    );
  }
}
