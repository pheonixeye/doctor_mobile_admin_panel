name: doctor_mobile_admin_panel
description: "doctor admin panel mobile port"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  pocketbase: ^0.24.0
  after_layout: ^1.2.0
  file_picker: ^11.0.2
  intl: ^0.20.2
  go_router: ^17.3.0
  equatable: ^2.0.7
  shared_preferences: ^2.3.5
  email_validator: ^3.0.0
  http: ^1.3.0
  cached_network_image: ^3.4.1
  supabase_flutter: ^2.8.3
  # flutter_dotenv: ^6.0.1
  flex_color_scheme: ^8.1.1
  flutter_colorpicker: ^1.1.0
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  flutter_local_notifications: ^22.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: "^0.14.3"

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
    - assets/fonts/
    # - assets/env/
